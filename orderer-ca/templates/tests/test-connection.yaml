apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "orderer-ca.fullname" . }}-test-connection"
  labels:
    {{- include "orderer-ca.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "orderer-ca.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

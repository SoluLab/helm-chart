apiVersion: v1
kind: Service
metadata:
  name: {{ include "orderer-ca.fullname" . }}
  labels:
    {{- include "orderer-ca.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orderer-ca.selectorLabels" . | nindent 4 }}

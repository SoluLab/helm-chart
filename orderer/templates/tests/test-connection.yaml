apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "peer.fullname" . }}-test-connection"
  labels:
    {{- include "peer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "peer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
